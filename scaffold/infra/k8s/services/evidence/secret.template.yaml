# ---------------------------------------------------------------------------
# TEMPLATE ONLY — contains NO real values. Do not commit a filled-in copy.
# Supply these from your secret manager (see infra/k8s/README.md):
#   - a Kubernetes Secret with these exact keys, or
#   - an ExternalSecret / SecretProviderClass that projects them.
# ---------------------------------------------------------------------------
apiVersion: v1
kind: Secret
metadata:
  name: evidence-secrets
  namespace: pmp-evidence
  labels:
    app.kubernetes.io/name: evidence
    app.kubernetes.io/part-of: pmp
type: Opaque
stringData:
  EVIDENCE_DB_OWNER_PASSWORD: REPLACE_ME
  EVIDENCE_DB_APP_PASSWORD: REPLACE_ME
  EVIDENCE_VAULT_KEY: REPLACE_ME
