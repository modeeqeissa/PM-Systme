{
  "name": "pmp-frontend",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev --workspace apps/web-portal",
    "build": "npm run build --workspace apps/web-portal",
    "test": "npm run test --workspace apps/web-portal"
  }
}
