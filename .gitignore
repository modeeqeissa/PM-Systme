# macOS / editor junk
.DS_Store
**/.DS_Store
~$*

# Claude Code local session state (not project source)
/.claude/

# original starter archive (not source)
pmp-claude-code-starter.zip

# python / service artefacts (belt-and-braces; each service also has its own .gitignore)
**/.venv/
**/__pycache__/
*.pyc
**/.pytest_cache/
**/.env
**/*.pem
**/.evidence-vault-dev/
